# RATE 消融实验设计（存档版，防上下文压缩）

- **日期**：2026-09-11
- **用途**：本文档是消融实验的唯一权威依据。若会话上下文被压缩，读这一篇即可恢复全部设计决策。
- **来源文档**（按权威度排序）：
  1. `TeRed RATE/M2论文主表_2026-09-08.md` —— 当前系统的最终实验口径（表1-4 全定稿）
  2. `TeRed RATE/B类投稿执行计划_2026-09-09.md` —— baseline + 消融 A1/A2/A3 的既定计划
  3. `论文研究/论文改造方案.md`（2026-08-21）—— 理论立意与贡献重排（注意：见 §6 故事线分歧）
  4. `论文研究/CLAUDE.md`、`TeRed RATE/results/` 各 REPORT、`.workbuddy/memory/2026-09-0*.md`

---

## 1. 当前系统（M2 口径）的模块清单

按数据流顺序：

| # | 模块 | 内容 | 代码/数据位置 |
|---|---|---|---|
| M1 | **TeRed 图归约** | 塌缩型归约，`share_k=-1`（整块吸收，主表口径）；`share_k≥0` 启用"共享对象保护"（`_shared_objects`：被 ≥k 个进程引用的节点不吸收） | `reduction/tered.py`；缓存 `cache/darpa/e5_f1_reduce/` |
| M2 | **RATE 拓扑编码**（本文核心增量） | 归约时汇总边带 **边质量 μ = 被折叠的内部边数**；RATE 用 **μ 加权的双通道度数** 编码（`encoding="rate"`），naive 用 **按边条数、无 μ** 的度数编码（`encoding="dual_naive"`） | `features/rate.py::node_feature_matrix`；`features/tape.py` |
| M3 | **v4 结构特征** | 3 列：log_deg / out_in_ratio / self_loop（nbr_type_div 已于 09-11 删除） | `features/v4extras.py` |
| M4 | **BenignEnsemble 检测器** | 良性集成 + 5% 分位半径（余弦）评分（非 GNN，轻量统计检测器） | `models/detector.py` |
| M5 | **告警聚合管线** | top-100 异常种子 → BFS 受限扩展（score≥P75，簇≤200 节点）→ 聚合分 → top-max_alerts 告警；**09-11 升级：纯 max 聚合 + 簇内最高分(cmax)排序** | `scripts/e6_alert_eval.py` |
| M6 | **预算与误报控制** | max_alerts=5 推荐预算；反事实纯良性 FPR 口径 | `scripts/e6c_benign_probe.py` |

### ⚠️ 三配置的真实差异（代码实锤，09-11 核对）

`scripts/e6_alert_eval.py` 的 configs 定义为：

| 配置 | 归约图 | 编码 | 真实差异 |
|---|---|---|---|
| `identity+rate` | 原图 | `rate` | 无归约 + RATE 编码 |
| `TeRed+naive` | TeRed 归约图 | `dual_naive` | 归约 + **无 μ** 度数编码 |
| `TeRed+RATE` | TeRed 归约图 | `rate` | 归约 + **μ 加权**双通道编码 |

即：**`TeRed+naive` 与 `TeRed+RATE` 的归约完全相同（都是 `share_k=-1`），唯一差异是节点拓扑编码有没有 μ**。
"共享感知折叠"对应 `share_k≥0` 的共享对象保护路径，**主表三配置全部未启用**。

`features/rate.py` 的原始设计意图（docstring 原文）：
- `dual_naive` 双通道、按边条数、无 μ ← naive 基线（**归约后应掉点**）
- `rate` 双通道 + μ 完整 RATE ← 主方法（**归约后不掉点**）

> ⚠️ **09-11 A5 实测推翻了这个设计意图**：`dual_naive` 并未在归约图上掉点（node 0.1694 vs `rate` 0.1692，逐位相等），μ 加权零收益。原因见 §4 的 μ 分布统计（μ≠1 边仅 0.05-0.07%，96.4% 节点编码完全相同）。
> **C1 定稿方向**：主故事改为「归约 + 双通道拓扑编码」（A5 已证编码必需、双通道优于单通道）；μ 作为负结果报告或压缩后重试。

评估口径：DARPA E5 cadets，GT=124 UUID（parse 口径），节点级 best_F1/P@50/PR_AUC + 告警级 alert_P/node_cov/F1_alert/first_hit，跨文件 mean±std。

## 2. 核心贡献（论文口径，对应消融）

| # | 贡献 | 证据 | 需要的消融 |
|---|---|---|---|
| C1 | **归约 + 双通道拓扑编码**：归约（TeRed）在固定编码下带来 +17% 告警 F1 与 +23% 覆盖度；节点拓扑编码**必需**（`none` 崩塌）且**双通道优于单通道**（node_F1 0.169 vs 0.092）<br>⚠️ **原设想的"μ 感知编码"已证无收益**（A5：`rate` 与 `dual_naive` 节点级逐位相等 0.1692 vs 0.1694；μ≠1 边仅占 0.05-0.07%，96.4% 节点编码完全相同）→ μ 作为负结果或压缩后重试 | identity/naive/RATE 三配置主表 + **A5 编码消融（已完成 09-11）** | ~~A2 share_k 曲线~~（降为可选附录） |
| C2 | **v4 结构特征**：节点级 best_F1 0.031→0.198（×6.4） | v3 vs v4 全量对比（15 格 14 格提升） | **A4**（逐列消融，已完成） |
| C3 | **告警聚合管线**：BFS 扩展使 node_cov ×1.3-4.4，best_F1_alert = 节点级 ×2.6 | flat vs clustered 对比（表2.3） | **A1**（管线解构，已完成） |
| C4 | **预算收敛 + 误报控制**：top-5 零损失、F1/精确率翻倍、FP≤5/日 | 表3（@20→@5）+ 表4（反事实 FPR） | **A3**（预算×排序，已完成） |

## 3. 已有的"免费"消融数据（不用重跑，直接进论文）

| 消融维度 | 已有数据 | 位置 |
|---|---|---|
| 归约 vs 无归约（identity/naive/RATE） | 全套 21 文件 × 3 配置 | M2 表1/2/3 |
| 特征 v3 vs v4 | 5 文件 × 3 配置全量 | `v4_validation_results/` |
| 无聚合（flat top-100）vs 聚合 | 5 文件对照 | M2 表2.3 |
| 预算 20 vs 5 | 21 行全量重跑 | M2 表3 |
| 纯良性误报（反事实口径） | bin.52/90 双模式 + 108 文件 GT 全扫 | M2 表4 |

## 4. 需要新跑的消融（按优先级）

### A1 · 告警聚合管线解构（最高优先，主表 C3 支柱）
- 配置（5 变体 × 11 文件）：① 去 BFS 扩展（只用种子）② 去簇聚合（种子直出告警）③ 去 top-B 排序（随机/自然序出告警）④ BFS 阈值 P50 / P90（默认 P75）⑤ 聚合权重变体（0.5·max+0.5·mean / 纯 max / 纯 mean）
- **成本关键**：种子分数已缓存，A1 大部分只是**重跑聚合管线**（秒-分钟级/文件），不必重算检测。先确认 `e6` 脚本可参数化再估工期。

### A5 · RATE 拓扑编码消融（**已完成 09-11，C1 的判定实验**）
- 固定 TeRed 归约图（`share_k=-1`），换节点拓扑编码：`rate`（μ+双通道）/ `dual_naive`（无 μ）/ `rate_single`（Σμ 单通道）/ `none`（纯语义+v4）
- 运行：`scripts/e6d_ablation_replay.py --groups A5`（仅对 `TeRed+RATE` 配置跑），1004s≈17min
- **结果（7 文件 mean）**：`dual_naive` node 0.1694 / alert 0.7123；`rate` node 0.1692 / alert 0.6610；`rate_single` node 0.0924 / alert 0.6858；`none` node 0.0068 / alert 0.0082
- **判定**：① 编码必需（none 崩塌）；② **双通道优于单通道**（0.169 vs 0.092）✅ 保留为贡献；③ **μ 加权零收益**（rate 与 dual_naive 节点级逐位相等，告警级反而略低）❌
- **μ 失效根因**：归约图 μ≠1 边仅 0.05-0.07%，受影响节点 3.3-3.6%，**96.4% 节点加权度≡计数度**（编码逐位相同）；且 μ 重尾（中位数 10、最大 10⁶），tape 编码饱和
- **可选补救**：`log1p(μ)` 或 P99 clip 压缩后重跑 A5（~17min），看能否救活 μ；否则 C1 改讲"归约 + 双通道编码"

### A2 · 共享感知折叠 share_k 曲线（折叠粒度，非主表口径）
- {naive, RATE} × share_k {1, 2, 4, 8}（-1 已有）
- 注意：`share_k≥0` 与 `-1` 是**两条不同实现路径**（前者启用共享对象保护 `_shared_objects`，且共享预算跨图全局、归约缓存失效）——不是同一方法的超参扫描，论文里要讲清
- 成本：share_k≠-1 需重跑归约（TeRed 归约每文件小时级），是**最贵**的一组 → 排后或减文件数
- 价值：若要把"共享感知折叠"作为卖点，这组必须有；若 C1 改走 μ 编码故事（A5），A2 降为可选（放"归约粒度敏感性"附录）

### A3 · 预算 × 排序（C4 支柱）
- max_alerts {1, 2, 3, 10}（5/20 已有）× 排序策略 {聚合分（默认），簇内最高分，节点 P95}
- 成本：全部基于已缓存分数重聚合，便宜

### A4 · v4 特征逐列消融（新增，C2 支柱）
- 逐列去掉 {log_deg, out_in_ratio, nbr_type_div, self_loop} 各跑一遍评分（4 变体 × 11 文件）
- 回答"×6.4 提升是哪一列带来的"，审稿人必问
- 成本：只重算检测评分（特征已缓存），便宜

## 5. 执行顺序建议

1. 先做 **A1 + A3 + A4**（全部基于缓存分数重聚合/重评分，1 天内可清完）
2. 再做 **A2**（需重跑归约，按服务器空闲排夜间）
3. baseline（Kairos/Flash/ThreaTrace 38-bin）已完成扫描归档，与消融无依赖

## 6. ✅ 故事线（09-11 二次修订：按代码实锤修正）

**09-11 09:03 初版**：用户拍板"跟实现走"，当时文档把 M2 实现描述为"共享感知折叠"。

**09-11 14:15 复核修正**（读 `e6_alert_eval.py::configs` + `features/rate.py` + `reduction/tered.py` 代码）：
- `TeRed+naive` 与 `TeRed+RATE` 的**归约完全相同**（均 `share_k=-1`），唯一差异是编码 `dual_naive`（无 μ）vs `rate`（μ 加权双通道）。
- 所以**本文 RATE 的真实实现 = 归约感知边质量 μ 的双通道度数编码**，"共享感知折叠"（`share_k≥0` 的共享对象保护）**主表未启用**。
- 这与 `features/rate.py` 的原始设计意图一致：naive 编码"归约后应掉点"，rate 编码"归约后不掉点"。

**修正后的论文主故事（推荐）**：
> TeRed 归约把共享实体/重复结构折叠成区域，**折叠会丢失边的质量信息**；RATE 编码用汇总边质量 μ（=被折叠的内部边数）加权双通道度数，把"这里折叠了多少"重新编码进节点特征，使检测器在归约图上**不掉点**；naive 编码（仅数边条数）则在归约图上显著掉点。

- C1 证据 = **A5 编码消融**（rate / dual_naive / rate_single / none）+ identity/naive/RATE 三配置主表。
- A2（share_k 曲线）降为可选：作为"归约粒度敏感性"附录，或在需要强调"共享感知"时再做。
- 改造方案里的 GNN/TopoGAT 部分仍作废（检测器是 BenignEnsemble）。

## 7. 环境备忘（执行时直接用）

- 服务器：`ssh user@your-server-host`（免密），GPU RTX 3080 10G，62G 内存
- baseline 结果：`TeRed RATE/results/{kairos,flash,threatrace}_full38/`（各含 REPORT.md + 阈值扫描）
- Kairos 38-bin 最佳 recall 75.8%@best-MCC；Flash 9.7%；ThreaTrace 排序反向
- Orthrus/RCAID/magic 已按用户决定搁置（OOM，emb_dim=128 需 >62G）
- 服务器已空闲（09-11 早确认）
- pgrep 自匹配坑：观察哨/诊断必须用 `mai[n]` 方括号写法，否则自杀或死锁
