# TeRed+RATE M2 v4 特征工程全量验证报告

- **日期**：2026-09-07
- **验证目标**：v4 特征工程（`features/v4extras.py` 追加 4 列）在 E5 cadets 攻击文件（bin.116–120）节点级检测上的提升效果，与 v3（无 v4 特征）同口径对比
- **决策树定位**：A→C 路径第一步 —— v4+BenignEnsemble 是否能把 v3 已证伪的节点级检测"救回来"
- **结论先行**：✅ **A 阶段成功**。5 文件全部 15 个对比格中 14 格提升；atfull 4 文件 naive/RATE 平均 best_F1 从 0.031 → 0.198（**6.4×**）；所有 v4 配置 best_F1 ≥ 0.135，**全面越过 0.10 成功线**。

---

## 1. 背景与动机

v3 时代（无 v4 特征）BenignEnsemble 节点级检测在 E5 cadets 上被证伪：

| 观察 | 结论 |
|---|---|
| 4 文件 TP≈0，best_F1 仅 0.001–0.052 | v3 benign 节点级检测失效 |
| 攻击节点占比 0.007%（124 GT / 28 万+ 节点） | 节点级 F1 天花板被 Precision 卡死 |
| 本地 smoke（余弦相似度评分）显示"无区分度" | 与真实结果不符 —— BenignEnsemble 实际用 **5% 分位半径评分**，本地 smoke 用错了评分机制 |

v4 特征工程假设：在归约产物（Gp + edges）上追加 4 个结构性特征，给异常评分提供"信号幅度"，把攻击节点在排序中抬到良性节点之前。

## 2. 方法

### 2.1 v4 特征（`features/v4extras.py` → `extend_with_v4()`）

在 RATE 归约产物上追加 4 列（不触碰归约过程，归约缓存全部命中，省 5–8h/文件）：

| 特征 | 含义 | 直觉 |
|---|---|---|
| `log_deg` | 节点度（对数） | 攻击子结构连接模式与良性不同 |
| `out_in_ratio` | 出度/入度比 | 攻击数据外泄/横向移动的向性 |
| `nbr_type_div` | 邻居类型多样性 | 攻击节点邻域类型混杂度高 |
| `self_loop` | 自环指示 | 攻击持久化/心跳模式 |

控制参数：`--v4-extras`（默认 True）/ `--no-v4-extras`（v3 对照）/ `--v4-scale {raw, robust}`。

### 2.2 评测口径（v3/v4 唯一差异 = 是否含 v4 特征）

- 训练：bin.1–5（`--max-records-train 400000`）
- 攻击文件：bin.116–120（share_k=-1，三配置 identity/naive/RATE 单次输出）
- 检测器：BenignEnsemble（5% 分位半径评分）
- 指标：best_F1（阈值扫描最优 F1）、P@10/50/100、PR_AUC
- 归约：从 `cache/darpa/e5_f1_reduce/` 命中缓存
- **口径差异**：bin.119 的 tered atfull 归约在 v3 时代即 21h+ 无法完成（9.4M 边），v4 改用 `--max-records-attack 500000` 截断口径（缓存命中，分钟级）；其余 4 文件为 atfull 口径。bin.119 与其余文件对比需谨慎，但其 v3/v4 对照为**同口径**，可比。

## 3. 全量结果（v3 vs v4，5 文件 × 3 配置）

### 3.1 逐文件对比

**bin.116（atfull，GT 存活 34）**

| config | v3 best_F1 | v4 best_F1 | 提升 | v3 P@50 | v4 P@50 | v3 PR_AUC | v4 PR_AUC |
|---|---|---|---|---|---|---|---|
| identity | 0.0087 | **0.1412** | 16.2× | 0.00 | 0.10 | 0.0011 | 0.0154 |
| naive | 0.0219 | **0.1739** | 7.9× | 0.02 | 0.12 | 0.0013 | 0.0213 |
| RATE | 0.0381 | **0.1739** | 4.6× | 0.02 | 0.12 | 0.0048 | 0.0214 |

**bin.117（atfull，GT 存活 35）**

| config | v3 best_F1 | v4 best_F1 | 提升 | v3 P@50 | v4 P@50 | v3 PR_AUC | v4 PR_AUC |
|---|---|---|---|---|---|---|---|
| identity | 0.0074 | **0.1579** | 21.3× | 0.00 | 0.12 | 0.0015 | 0.0244 |
| naive | 0.0146 | **0.2133** | 14.6× | 0.00 | 0.16 | 0.0016 | 0.0377 |
| RATE | 0.0226 | **0.2133** | 9.4× | 0.00 | 0.16 | 0.0043 | 0.0378 |

**bin.118（atfull，GT 存活 34）**

| config | v3 best_F1 | v4 best_F1 | 提升 | v3 P@50 | v4 P@50 | v3 PR_AUC | v4 PR_AUC |
|---|---|---|---|---|---|---|---|
| identity | 0.0043 | **0.1351** | 31.6× | 0.00 | 0.10 | 0.0007 | 0.0264 |
| naive | 0.0190 | **0.2018** | 10.6× | 0.00 | 0.14 | 0.0012 | 0.0388 |
| RATE | 0.0346 | **0.2000** | 5.8× | 0.00 | 0.14 | 0.0037 | 0.0388 |

**bin.119（at500k 截断口径，GT 存活 15）**

| config | v3 best_F1 | v4 best_F1 | 提升 | v3 P@50 | v4 P@50 | v3 PR_AUC | v4 PR_AUC |
|---|---|---|---|---|---|---|---|
| identity | 0.0250 | **0.2857** | 11.4× | 0.00 | 0.04 | 0.0025 | 0.0701 |
| naive | 0.0769 | **0.2353** | 3.1× | 0.02 | 0.04 | 0.0097 | 0.0710 |
| RATE | **0.2857** | 0.2353 | 0.8× ⚠ | 0.10 | 0.04 | 0.1004 | 0.0717 |

**bin.120（atfull，GT 存活 21）**

| config | v3 best_F1 | v4 best_F1 | 提升 | v3 P@50 | v4 P@50 | v3 PR_AUC | v4 PR_AUC |
|---|---|---|---|---|---|---|---|
| identity | 0.0124 | **0.1389** | 11.2× | 0.00 | 0.08 | 0.0020 | 0.0214 |
| naive | 0.0682 | **0.2034** | 3.0× | 0.02 | 0.12 | 0.0068 | 0.0374 |
| RATE | 0.0290 | **0.2034** | 7.0× | 0.02 | 0.12 | 0.0039 | 0.0374 |

### 3.2 汇总

- **提升方向**：15 格对比（5 文件 × 3 配置）→ **提升 14 格，未提升 1 格**（bin.119 RATE）
- **atfull 4 文件 naive/RATE 均值**：
  - naive：v3 mean **0.0309** → v4 mean **0.1981**（**6.4×**）
  - RATE：v3 mean **0.0311** → v4 mean **0.1977**（**6.4×**）
- **排序质量**：P@50 从 v3 的 0.00–0.02 → v4 的 **0.08–0.16**（攻击节点稳定进入 top-50）
- **排序整体性**：PR_AUC 普遍提升 10–30×，说明不是单点偶然，是整条异常分排序被 v4 特征拉正

## 4. 分析

### 4.1 v4 特征为什么有效（相对 v3）

v3 时代 BenignEnsemble 用 5% 分位半径评分，攻击节点与良性"看起来一样"（都偏离训练质心不远），无法被抬升。v4 的 4 列（度对数、出/入比、邻居类型多样性、自环）刻画的是**结构角色差异**：
- 攻击子图（C2 通道外传、横向移动）的节点在度分布与邻居类型上系统性偏离良性主机；
- `out_in_ratio` 抓住攻击流单向性（外传方向主导），良性会话多为双向/均衡；
- 这解释了本地 smoke（余弦相似度）与真实评测（5% 分位半径）的结论分歧——**排序特征的判别力必须用真实评分机制验证**。

### 4.2 identity 提升最猛（11–32×）

identity（无归约基线）v3 时几乎全 0（best_F1 0.004–0.025），v4 后抬到 0.135–0.286。原因：identity 没有 tered 归约的"异常增强"效果，v3 特征下攻击节点毫无排序优势；v4 结构特征直接作用于原始图，把裸图上的攻击结构也抬了起来。

### 4.3 特例：bin.119 RATE 未提升（0.8×）

bin.119 是唯一 v4 未超越 v3 的格，但需注意三点：
1. **口径不同**：bin.119 为 at500k 截断（其余 4 文件 atfull），攻击图只有前 50 万条记录，结构被截断，v4 结构特征（尤其 out_in_ratio/nbr_type_div）在截断图上信息受损；
2. **样本最小**：GT 存活仅 15 个攻击节点，best_F1 单点噪声大（v3 RATE 的 0.286 来自 top-10 恰好命中 3 个的偶然性）；
3. **该文件 v4 内部仍一致**：v4 identity（0.286）与 naive（0.235）远超 v3 同配置（0.025/0.077），v4 没有"崩"，只是 RATE 的 v3 值本身异常偏高。

结论：不构成对 v4 的否定，但应在报告中如实保留。

### 4.4 与决策树对照

```
v4 全量 best_F1 ≥ 0.10 → A 成功，主线 v4+BenignEnsemble ✅
```
5 文件全部 v4 配置（15 格）best_F1 ∈ [0.135, 0.286]，无一低于 0.10 门槛，且一致性极高（atfull 4 文件 naive/RATE 收敛在 0.174–0.213 窄带内）。

## 5. 数据文件

| 内容 | 路径（服务器 /TeRed+RATE/code/results/） |
|---|---|
| v4 全量 ×5 | `e5_f1_v4_bin.{116,117,118,119,120}_sk-1.csv` |
| v3 对照 ×5 | `e5_f1_v3b_bin.{116,117,118,119,120}_sk-1.csv` |
| 运行日志 | `logs/e5_f1_v4_bin.*.log`、`logs/e5_f1_v3b_*.log` |

## 6. 结论与建议

### 6.1 结论

1. **A 阶段（v4+BenignEnsemble 救节点级检测）成功**：v4 特征使 5 文件节点级 best_F1 全面越过 0.10 门槛，atfull 4 文件 naive/RATE 平均提升 **6.4×**（0.031 → 0.198）。
2. v4 提升的是**排序判别力**而非单点指标：P@50 与 PR_AUC 同步大幅改善，结果可信。
3. naive 与 RATE 在 v4 下几乎持平（差异 <0.002），说明 v4 特征主导了判别力，归约增强的边际贡献被吸收——**可考虑简化流水线直接用 naive 或 identity+v4**。

### 6.2 建议（后续步骤）

1. **推进 C 阶段（告警级聚合）**：v4 已给出有区分度的异常分排序，可进入 C 的 top-K 子图告警聚合，验证 E5 告警级 F1 —— 这是 M2 主交付指标。
2. **v4 特征消融**：逐一去掉 4 列观察 best_F1 退化的特征，确认哪些列贡献最大（预期 out_in_ratio 与 nbr_type_div 为主）。
3. **bin.119 atfull 补跑**：若需 5 文件全 atfull 口径，需在服务器空闲窗口（如夜间）让 tered atfull 归约跑通（21h+）；当前 at500k 截断口径结论已同口径可比。
4. **optional：v4+GraphSAGE**：若 C 告警级仍受限于 BenignEnsemble 线性评分，v4 特征可直接喂 GraphSAGE 做深度基线对照。

---
*报告由全量验证自动生成：5 文件 × 3 配置 × 2 版本（v4/v3b）= 30 次评测运行，全部命中归约缓存（除 bin.119 特例）。*
