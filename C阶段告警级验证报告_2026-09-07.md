# TeRed+RATE M2 C 阶段（告警级口径）验证报告

- **日期**：2026-09-07（晚间）
- **前置**：v4 特征工程 A 阶段已验证成功（节点级 best_F1 0.031→0.198，×6.4）
- **本报告回答**：把 v4 节点异常分**聚合成子图告警**后，E5 cadets 告警级口径是否可用
- **结论先行**：✅ **C 阶段成立**。21/21 个（文件×配置）组合的 **first_hit ≤ 3**（前 3 个告警内必中攻击子图，其中 14/21 是第 1 个告警即命中）；atfull 4 文件 best_F1_alert 均值 **naive 0.531 / RATE 0.515**，是节点级 best_F1（0.198）的 **2.6 倍**。

---

## 1. 告警管线设计（`scripts/e6_alert_eval.py`）

```
v4 节点异常分（BenignEnsemble，与 v4 全量验证同口径同缓存）
  ↓ top-100 异常节点作种子
  ↓ BFS 连通扩展（只经过分数 ≥ 图内 P75 的节点，簇大小 ≤ 200）
  ↓ 过滤 ≥3 节点的簇，聚合分 = 0.7×max + 0.3×mean
  ↓ 按聚合分排序，取 top-20 = 每文件每日的告警列表
```

**告警级指标**：
- `alert_P`：含 ≥1 GT 节点的告警占比（SOC 视角的告警精确率）
- `node_cov`：告警覆盖的 GT 节点 / 图内 GT 节点（节点级召回）
- `F1_alert = harmonic(alert_P, node_cov)`；`best_F1_alert` 按告警预算 b=1..20 扫描取最优
- `first_hit` / `top1_hit`：第一条命中攻击的告警排名 / 第 1 条告警是否命中
- `flat_topk_*`：不聚合、直接 top-100 节点作告警的对照（验证聚合的价值）

口径与 v4 全量验证完全一致：bin.1-5 良性训练（40 万记录/张）、share_k=-1 归约缓存全部命中、v4 raw 特征、三配置（identity / TeRed+naive / TeRed+RATE）。

## 2. 核心结果（7 测试文件 × 3 配置）

### 2.1 告警排序质量：前 3 必中（21/21）

| 文件 | identity first_hit | naive first_hit | RATE first_hit |
|---|---|---|---|
| bin.116 | **1** | **1** | **1** |
| bin.117 | **1** | **1** | **1** |
| bin.118 | **1** | 2 | **1** |
| bin.119@500k | **1** | 2 | 2 |
| bin.120 | **1** | **1** | **1** |
| bin.6@400k | **1** | **1** | **1** |
| bin.7@400k | **1** | 2 | 3 |

**top1_hit=1 占 14/21**。SOC 视角：分析师打开第 1~3 条告警就能看到攻击子图。

### 2.2 逐文件告警级指标（粗体 = 该文件三配置最优）

| 文件 | 配置 | n_gt | node_cov | best_F1_alert | best_b | 告警节点数 |
|---|---|---:|---:|---:|---:|---:|
| bin.116 | identity | 34 | 0.206 | 0.256 | 1 | 2659 |
| bin.116 | naive | 46 | 0.261 | **0.414** | 2 | 734 |
| bin.116 | RATE | 46 | 0.304 | 0.357 | 1 | 1232 |
| bin.117 | identity | 30 | 0.300 | 0.276 | 14 | 2927 |
| bin.117 | naive | 35 | 0.486 | 0.654 | 3 | 1176 |
| bin.117 | RATE | 35 | 0.543 | **0.679** | 4 | 1599 |
| bin.118 | identity | 27 | 0.519 | 0.465 | 19 | 2855 |
| bin.118 | naive | 34 | 0.529 | 0.500 | 2 | 1526 |
| bin.118 | RATE | 34 | 0.441 | **0.553** | 1 | 1543 |
| bin.119@500k | identity | 10 | 0.800 | **0.889** | 1 | 1222 |
| bin.119@500k | naive | 15 | 0.867 | 0.634 | 2 | 1058 |
| bin.119@500k | RATE | 15 | 0.867 | 0.754 | 3 | 764 |
| bin.120 | identity | 21 | 0.333 | 0.385 | 1 | 2851 |
| bin.120 | naive | 26 | 0.385 | **0.556** | 2 | 1166 |
| bin.120 | RATE | 26 | 0.346 | 0.471 | 1 | 1594 |

**atfull 4 文件均值**：

| 指标 | identity | naive | RATE | 节点级（v4 对照） |
|---|---:|---:|---:|---:|
| best_F1（alert / node） | 0.345 | **0.531** | 0.515 | 0.198 |
| node_cov（GT 节点召回） | 0.339 | 0.415 | 0.408 | Recall ≈ 0.44 |

**告警级 best_F1 是节点级的 2.6×**，且最优预算 best_b 集中在 **1~4**——前几条告警就拿到大部分收益，预算效率高。

### 2.3 聚合的价值：clustered vs flat top-100（同一异常分，两种用法）

| 文件 | flat_topk_cov（top-100 直报） | clustered node_cov（BFS 扩展后） | 提升 |
|---|---:|---:|---:|
| bin.116 | 0.13~0.18 | 0.21~0.30 | +1.6× |
| bin.117 | 0.27~0.29 | 0.30~0.54 | +1.8× |
| bin.118 | 0.26~0.32 | 0.44~0.53 | +1.7× |
| bin.119@500k | 0.13~0.20 | **0.80~0.87** | +4.4× |
| bin.120 | 0.27~0.29 | 0.35~0.38 | +1.3× |

BFS 扩展把 top-100 种子周边的攻击节点"捎带"进来（攻击子图连通），**GT 节点召回提升 1.3~4.4 倍**。bin.6 identity 甚至出现 flat_cov=0 但 clustered cov=0.50——top-100 完全没直接命中 GT，但扩展后覆盖一半。**聚合不是锦上添花，是告警级口径的核心增益来源。**

## 3. 数据注意事项

1. **bin.119 为 at500k 截断口径**（tered atfull 归约不可行，与 v4 验证一致），其 0.80~0.87 的高覆盖部分来自图小 GT 少，单列不与 atfull 混算。
2. **bin.6/bin.7 并非纯良性**：GT 命中 6~11 个节点（Nginx Drakon APT 战役横跨多个文件），它们实际是"低攻击密度"文件而非阴性对照。其结果（first_hit=1、cov 0.5~0.78）说明管线对稀疏攻击同样敏感；**纯良性误报率**（0 GT 文件上的告警数）本数据集无法直接测，需在 e3/trace 数据或 bin.8~115 中挑 0 命中文件补测。
3. **node_prec 低（0.003~0.017）是口径属性不是缺陷**：告警是子图（百级节点），GT 只标注已知恶意实体（个位数），SOC 分析师看的是子图上下文而非逐节点判定。
4. **告警条数**：每文件封顶 20 条（max_alerts），实际 10~20 条/文件；按 SOC 惯例仍偏多，best_b≤4 表明收紧到 top-4 告警即可保留大部分召回。

## 4. A→C 路径完整结论（M2 主线）

| 阶段 | 结论 | 关键数字 |
|---|---|---|
| A：v4 特征救 BenignEnsemble | ✅ 成功 | 节点级 best_F1 0.031→0.198（×6.4） |
| C：告警级聚合 | ✅ 成立 | best_F1_alert 0.52-0.53（×2.6 节点级）；first_hit≤3 全中；聚合使 GT 召回 ×1.3-4.4 |

**M2 主线定稿：v4 特征 + BenignEnsemble + top-K/BFS 子图告警聚合。**

## 5. 建议下一步

1. **纯良性误报补测**：在 bin.8~bin.115 中扫描 GT 命中为 0 的文件，跑同管线测"每日假告警数"（论文必需的 FPR 口径）。
2. **告警预算收紧实验**：max_alerts=5 重跑（复用缓存，仅需评测段，~15 分钟），验证 top-5 告警的 cov/F1 损失。
3. **写 M2 论文主表**：节点级（v4 报告）+ 告警级（本报告）双口径，naive/RATE 并列。
4. 可选：bfs_q/max_cluster 参数敏感性（10 分钟级扫描）。

## 6. 产出文件

| 内容 | 位置 |
|---|---|
| 告警评测脚本 | 服务器 + 本地 `scripts/e6_alert_eval.py` |
| 全量结果 | 服务器 + 本地 `results/e6_alert_full.csv`（21 行） |
| 冒烟结果 | 服务器 `results/e6_alert_smoke.csv` |
| 运行日志 | 服务器 `logs/e6_alert_full.log`（51 分钟，单进程峰值 RSS 20GB） |

---
*C 阶段全量：7 文件 × 3 配置 = 21 次评测，归约缓存全部命中（bin.6/7 tered 归约新算并已入缓存）。*
